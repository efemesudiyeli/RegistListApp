import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efemesudiyeli.regist',
  appName: 'regist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
